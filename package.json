{
  "name": "redux-playground",
  "version": "0.0.1",
  "description": "Redux playground",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf lib",
    "prebuild": "npm run clean",
    "build": "babel src --out-dir lib",
    "start": "nodemon --watch src -e js,jsx --exec babel-node src/index",
    "serve": "node lib/index"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.3.14",
    "express": "^4.13.3",
    "lodash": "^3.10.1",
    "react": "^0.14.3",
    "react-dom": "^0.14.3",
    "react-redux": "^4.0.2",
    "redux": "^3.0.4",
    "redux-thunk": "^1.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13",
    "nodemon": "^1.8.1"
  }
}
